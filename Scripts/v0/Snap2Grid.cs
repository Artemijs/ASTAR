using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Snap2Grid : MonoBehaviour
{
	//public Main main;
	/*GridManager _grid;
	Vector3 _lastPos;
	Tile _cNode;
    // Start is called before the first frame update
    void Start()
    {
		_grid = GameObject.Find("Main").GetComponent<Main>().GetGridMgr();
		_lastPos = this.transform.position;
		_cNode = _grid.GetTile(_lastPos);
		Debug.Log("STARTING");
	}

    // Update is called once per frame
    void Update()
    {
		Snap();
		
    }
	void Snap() {
		_cNode = _grid.GetTile(this.transform.position);
		this.transform.position = _cNode.gameObject.transform.position;
	}
	public Tile GetNode() { return _cNode; }*/
}
